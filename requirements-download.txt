# Requirements for ComfyUI Model Download CLI Tool
# Install with: pip install -r requirements-download.txt

# Core dependencies (should already be available in ComfyUI)
# urllib3 - for HTTP requests
# pathlib - for path handling

# Optional but recommended for full functionality
huggingface_hub>=0.19.0  # For downloading from Hugging Face Hub
tqdm>=4.64.0  # For better progress bars
requests>=2.28.0  # For HTTP requests with better error handling

# For model validation and metadata
safetensors>=0.3.0  # For reading SafeTensors metadata

# For Civitai integration
requests>=2.28.0  # For Civitai API calls (already listed above)
